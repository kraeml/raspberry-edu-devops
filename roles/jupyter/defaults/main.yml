---
# defaults file for jupyter
dependencies_packages:
    - libncurses5-dev
    - python-dev
    - pandoc

pip3_packages:
    - jupyter
    - readline
    - ipyparallel
    - bash_kernel
    - ipython-sql
